# Playwright TS Framework

Basic Playwright + TypeScript starter framework with:

- Page Object Model (`pages/`)
- Custom fixtures (`fixtures/`)
- Utilities (`utils/`)
- Test data (`test-data/`)
- Sample test (`tests/`)

## Setup

```bash
npm install
npx playwright install
```

## Run tests

```bash
npm test
```
